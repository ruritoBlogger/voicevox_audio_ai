mutation addComment($content: String!, $author: CommentAuthorType!) {
    addComment(data: {content: $content, author: $author}) {
        id
        author
        content
        createdAt
        updatedAt
    }
}